#include "procesar_funcion.h"


int main() {
    leer_proceso_desde_archivo("procesos.txt");
    // Descomentanto acá puede elegir que opción de prueba procesar
    // Puede de hecho, procesar las dos al mismo tiempo descomentando las dos
    // leer_proceso_desde_archivo("procesos_2.txt");
    return 0;
}